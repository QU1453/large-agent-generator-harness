# LAG harness UI 设计修改方案

> 版本：v1 ｜ 日期：2026-08-21
> 依据：对 VSCode / Trae / Claude Code 界面设计语言的拆解分析 + 用户偏好确认
> 实施约束：**零功能改动、零 C 盘写入、不修改任何原有文件**（仅新增/重写 `src/design-overlay.css` 纯视觉覆盖层）

---

## 1. 背景与目标

当前界面为"极光 Aurora"暗色体系（紫罗兰 `#8b7cf6` → 青色 `#38d6c4` 双强调色）。用户反馈：
1. **不喜欢现在的颜色**——紫+青双强调色偏"冷艳花哨"，缺少品牌辨识度；
2. **希望参考动画、渐变、动态**——要有渐变质感与克制的动效；
3. **品牌形象未定义**——喜欢**橙色暖色、猫**这类形象，要求品牌元素重新设计，用强渐变发光。

目标：把界面从"紫色极光"改造成 **「冷静专业的蓝靛工作台 + 温暖橙猫品牌性格」**——可靠的工程师工具感 + 有温度的伙伴感，同时保持专业克制。

---

## 2. 现状问题分析

| 问题 | 说明 |
|---|---|
| 双强调色冲突 | 紫罗兰与青色同为高饱和强调色，互相抢注意力，界面显得"花" |
| 品牌缺失 | Logo 是闪电 ⚡ + 渐变方块，无品牌故事；主按钮/激活态全是同一渐变，没有"品牌高光"层次 |
| 硬编码色值 | styles.css 中约 28 处硬编码 `rgba(139,124,246,…)`，换色必须逐一迁移 |
| 动效保守 | 已有悬停/入场基础动效，但缺少品牌元素的生命感（发光呼吸/渐变流动） |

---

## 3. 设计方向（概念）

> **概念一句话**：冷静的蓝靛工作室，一只温暖橙猫在工作台上发着光。

采用**三层配色结构**，各司其职，避免颜色打架：

| 层级 | 角色 | 色相 | 用途 |
|---|---|---|---|
| 基调层 | 界面底色/边框/文字 | 蓝石墨（偏蓝的深灰） | 不抢内容，长时间使用舒适 |
| 交互层 | 选中/聚焦/激活/状态 | 蓝靛（indigo） | 交互反馈，克制、专业（VSCode 式） |
| 品牌层 | Logo/主按钮/头像/空状态 | 暖橙（orange） | 品牌高光，强渐变发光（Claude 式温暖） |
| 信息层 | 工具调用/标签/提示 | 青（teal-cyan） | Agent 活动指示，冷色不喧宾夺主 |
| 语义层 | 成功/警告/错误 | 绿/黄/红 | 状态编码，不参与美化 |

---

## 4. 色板详表（token 对照）

### 4.1 基调层（蓝石墨）
| token | 旧值 | 新值 | 用途 |
|---|---|---|---|
| `--bg-0` | #090b10 | #070a12 | 最深底（主背景） |
| `--bg-1` | #0d1017 | #0b101c | 侧栏/面板底 |
| `--bg-2` | #12161f | #101626 | 卡片/气泡底 |
| `--bg-3` | #181d28 | #171f33 | 输入/悬浮底 |
| `--bg-4` | #202635 | #1f2a45 | 按钮底 |
| `--bg-5` | #2a3142 | #2b3a5e | 悬停高亮 |
| `--border` | #232b3a | #1e2942 | 常规边框 |
| `--border-2` | #303a4d | #2b3a5c | 强边框 |
| `--border-strong` | #3d4a63 | #3d5080 | 最强边框/滚动条 |
| `--text` | #e8eef7 | #e9eef9 | 主文字 |
| `--text-2` | #a3aec2 | #a7b2c9 | 次文字 |
| `--text-3` | #67748c | #6b7895 | 弱文字 |
| `--text-4` | #48536a | #48546f | 最弱/占位 |

### 4.2 交互层（蓝靛）
| token | 旧值 | 新值 | 用途 |
|---|---|---|---|
| `--violet` | #8b7cf6 | #6d8dff | 聚焦/选中/激活/链接 |
| `--violet-2` | #6d5ce0 | #4a6cf5 | 更深交互色 |
| `--blue` | #6ea8ff | #7aa2ff | 工作区链接 |
| `--focus` | 2px rgba(139,124,246,.45) | 3px rgba(109,141,255,.30) | 焦点环 |
| `::selection` | rgba(139,124,246,.35) | rgba(109,141,255,.38) | 文本选区 |

### 4.3 信息层（青，微调更偏蓝）
| token | 旧值 | 新值 |
|---|---|---|
| `--cyan` | #38d6c4 | #40c8d8 |
| `--cyan-2` | #22b8a8 | #2eb6c8 |

### 4.4 品牌层（暖橙，新增）
| token | 值 | 用途 |
|---|---|---|
| `--brand` | #ffa24c | 品牌橙（亮） |
| `--brand-2` | #ff6a3d | 品牌橙（深，渐变末端） |
| `--brand-grad` | `linear-gradient(135deg, #ffa24c, #ff6a3d)` | 品牌渐变（Logo/主按钮/发送/头像） |
| `--brand-glow` | `0 4px 16px rgba(255,122,61,.45)` | 品牌发光 |
| `--brand-text-grad` | `linear-gradient(90deg, #fff, #ffd9b8)` | 标题文字渐变 |
| `--grad` | 紫→青 | **改为品牌渐变**（`--brand-grad`） |
| `--grad-soft` | 紫青软 | **改为靛色软** `rgba(109,141,255,.14)`（导航激活底） |

> 关键决定：`--grad` 重定义为品牌橙渐变——主按钮、发送按钮、头像、Logo、开关全部自动变橙；
> `--grad-soft` 保持冷色，让导航/激活区安静，橙只出现在品牌高光处，**控制橙色面积，避免喧宾夺主**。

### 4.5 语义层（保留微调）
`--danger #ff6b6b`、`--success #4ade80`、`--warning #fbbf24` 基本保留，文本色走 v2 已建的 `--danger-text/--success-text/--warning-text`。

---

## 5. 品牌形象：暖橙猫（纯 CSS 实现，零 JSX 改动）

| 元素 | 现状 | 改造 |
|---|---|---|
| 侧栏 Logo 徽标 | ⚡ + 紫青渐变方块 | 橙渐变 + 橙发光 + **emoji 换成 🐱**（`font-size:0` 隐藏 ⚡，`::before` 居中渲染 🐱；保留玻璃高光 `::after`） |
| Logo 标题 | 白→灰紫渐变字 | 白→暖橙浅渐变字 |
| 空状态大图标 | ⚡ + 紫渐变 + 浮动动画 | 🐱（同上技巧）+ 橙渐变 + **呼吸发光动画** |
| 空状态标题 | 白→灰紫渐变字 | 白→暖橙浅渐变字 |
| 运行转圈 | 紫 | 蓝靛（保持交互层冷色） |

> 🐱 emoji 本身即橙色猫脸，天然契合"橙猫"形象；后续如需专属插画，可在此位置替换为 SVG/图片而不改结构。

---

## 6. 渐变与发光方案（强渐变发光）

**品牌橙强发光（high impact，限量）**：
- Logo 徽标（橙渐变 + 发光 + 呼吸脉动）
- `.btn.primary` / `.btn.send`（橙渐变 + 悬停发光增强 + 微浮起）
- `.msg-avatar`（橙渐变 + 橙光晕）与用户气泡（橙调渐变）
- 空状态大图标（橙渐变 + 呼吸发光）
- `.toggle.on`（橙渐变，跟随 `--grad` 自动生效）

**蓝靛柔和（低饱和，普遍）**：焦点环、输入聚焦、选中节点/连线/会话、导航激活指示、chip 激活、spinner、trace 代码底色、团队 AI 消息。

**青色信息（保持）**：工具调用状态条、`.tag.tool`、hint 高亮、工作流连线。

---

## 7. 动效方案（轻量克制）

| 动效 | 描述 | 时长 | 备注 |
|---|---|---|---|
| 品牌呼吸发光 | Logo/空状态图标 box-shadow 缓慢脉动 | 4s 循环 | 橙光"呼吸"，品牌生命感 |
| 主按钮悬停 | 微浮起 + 发光增强 | 0.14s | 已有，改橙色 |
| 按压回弹 | translateY(1px) | 0.08s | 已有 |
| 消息入场 | 淡入 + 上移 6px | 0.22s | 已有 `msg-in` |
| 激活指示条 | 背景/指示条过渡 | 0.14s | nav/session |
| 卡片悬停 | 抬升 -2px + 阴影 | 0.16s | 已有 |
| 氛围光漂移（可选） | 主区背景光晕缓慢漂移 | 24s 循环 | 极淡，reduced-motion 关闭 |
| 流式光标 | 现有渐变光标闪烁 | — | 保留 |

原则：**时长全部 ≤0.22s（除循环光效），只响应状态改变**；`prefers-reduced-motion` 下全部关闭。

---

## 8. 实施方式与约束

- **唯一改动文件**：`src/design-overlay.css`（v3 全量重写，纯 CSS）
- **零改动**：`styles.css`、所有 JSX、electron 端、数据文件
- **接入点**：`src/main.jsx` 已有 `import './design-overlay.css'`（上一轮已加，无需再动）
- **一键还原**：删除该 import 行
- **硬编码迁移**：styles.css 中约 28 处 `rgba(139,124,246,…)` 在覆盖层中逐一以新色覆盖（品牌元素→橙、交互元素→靛）
- **验证**：`npm run build` 通过、产物含新色板 token 与 🐱 样式

---

## 9. 逐项修改清单（selector 对照）

**A. token 重定义（:root）**：见第 4 节全表。

**B. 品牌橙迁移（硬编码紫 → 橙）**：
`.logo-badge`、`.btn.primary`、`.btn.primary:hover`、`.btn.send`、`.btn.send:hover`、`.msg-avatar`、`.msg-bubble.user`、`.empty-logo`、`.agent-chip:hover`、`.chat-empty h2`、`.logo-title`。

**C. 交互靛迁移（硬编码紫 → 靛）**：
`::selection`、`.session-item.active`、`.trace-head code`、`.composer-box:focus-within`、`.input:focus`、`.agent-card:hover`、`.agent-code`、`.chip.active`、`.wf-edge-g.selected`、`.wf-node.selected`、`.nav-spinner`、`.wf-spinner`、`.wf-port.top`（及 hover）、工作流画布选中填充、`.team-msg-ai`。

**D. 品牌形象**：`.logo-badge` / `.empty-logo` 的 🐱 替换 + 呼吸发光 keyframes。

**E. 保留 v2 成果**：圆角体系、焦点环、三级按钮、语义文本色、滚动条、toast 不拦截点击、等宽元信息。

---

## 10. 风险与回退

| 风险 | 应对 |
|---|---|
| 橙色面积过大显花 | 橙仅限品牌层 6 类元素，激活/聚焦一律靛色；可再通过调 `--brand-grad` 色值微调 |
| emoji 猫与 Windows 字体渲染差异 | 🐱 为系统通用 emoji，各平台一致；渲染偏差时可换 😺/🐈 |
| 动效影响性能 | 循环动画仅 2 处（呼吸发光/可选光晕漂移），且都轻量（box-shadow/transform），reduced-motion 关闭 |
| 需要完全回退 | 删除 `design-overlay.css` 的 import 即回到 Aurora 原版 |
