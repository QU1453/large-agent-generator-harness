agent调工具
把每个工具写成一份说明书
结构化调用，llm生成请求
执行器，调函数
返回结果
继续推理

工业级
工具匹配，参数抽取，数据校验，沙箱执行，上下文融合

六种工程化调用
react             思考行动循环
langchanin编排    复杂决策链，串联多个工具
小模型裁判      ，判断走哪条路
时间总线异步      高并发低延迟异步
本地插件          本地一把梭哈
多agent协作       互发消息，委托任务

llm怎么判断要不要调工具
提示词定规矩
训练小模型判断
react规划

mcp
全用json schema说话

上下文断了怎么办
滑动窗口，摘要压缩，向量检索，top-k打分

rag链路怎么跑
用户问题--转成向量坐标--ann检索--拼接--llm生成

记忆
短期记忆
工作记忆（常用）
长期记忆（持久化）

多agent
通信协议协商
广播或者路由：谁写文档写得好？？

任务状态传递方法，有限状态机，actor模型，事件溯源

图增强检索灵活度低且难以维护，但是效果比传统向量库检索好很多