# LAG harness 整体方案（v0.1 草案）

> 依据：`开发文档.txt`、`记忆管理-开发文档.txt`、`MCP.txt`、`学习与适应.txt`
> 状态：**草案，待评审**。本文只做方案与规划，评审通过后再进入实施。

---

## 一、产品定位

**LAG harness = 大型 Agent 生成器（Large Agent Generator）**

把可视化工作流"编译"成一个**自包含的大型 Agent（LA）包**，可导出为本地运行程序（最终形态：exe，双击即用、自带极简交互界面），供非程序员"动动鼠标"接入其它软件 / 网页 / Agent 生态。

核心链路：

```
工作区/会话 配置(工作流/智能体) → 聊天窗口输入需求 → 智能体/工作流执行 → 导出为 LA 包(exe)
```

---

## 二、目标流程（来自开发文档）

| # | 需求 | 现状 | 差距 |
|---|------|------|------|
| 1 | 工作流可导出为 **.exe 本地程序**，自带极简交互界面 | ✅ 已实现导出 Python 包（`manifest.json` + executor + transports + web 控制台 + 内嵌 runtime + `start.bat`），跑通链路 | ⚠️ 目前是"Python 包 + 启动脚本"，不是单个 exe；需增加 exe 包装（PyInstaller 一键打包或 NSIS 封装） |
| 2 | **工作区可对话**：工作区→配置工作流/智能体→聊天窗口输入需求→进入执行 | ✅ 已实现（工作区底部对话栏：选智能体/工作流 → 发消息 → 流式回复） | — |
| 3 | 接入 LLM 需用户提供 API key；harness 内可同步已保存 key；**导出后必须手动输入** | ✅ 全局设置保存 baseUrl/key；导出物 manifest 用 `env:` 占位符 | 补充：导出物首个对话前弹 API key 配置页，并持久化到本地（M2-⑤） |
| 4 | **会话可自由选择智能体 / 工作流（大型智能体）**，配置可保存 | ✅ 会话级目标选择（智能体/工作流）已实现，保存到会话元数据 | — |
| 5 | **导出必须附带记忆空间** | ❌ 无 | 见"四、记忆管理"；导出物自带 memory 目录 + 记忆读写工具（M2-⑦） |
| 6 | 会话展示**可展开的思考痕迹** | ✅ 已实现（assistant 消息带 trace，`<details>` 折叠展示工具/节点明细） | — |
| 7 | **模型选择要正常**（如 deepseek v4 pro/flash） | ✅ 全局默认模型 + 会话/智能体级覆盖；预置 `deepseek-v4-pro` / `deepseek-v4-flash` | — |

---

## 三、架构分层（大分层：会话 / 工作区）

> 分层原则（用户定调，2026-08-19）：顶层只分 **会话** 与 **工作区** 两大块；除聊天窗口外，功能模块分类为 **智能体 / 工具 / MCP / 工作流（大型智能体）/ 记忆管理 / MCP 设计**；**智能体学习不纳入本架构**，属后续智能体设计内容。

```
┌──────────────────────────────────────────────────┐
│ LAG harness（大型 Agent 生成器）                        │
│                                                    │
│  ┌────────────┐        ┌───────────────────────┐   │
│  │  会话        │        │ 工作区                 │   │
│  │  ─ 聊天窗口   │        │  ├ 智能体              │   │
│  │  ─ 思考痕迹   │        │  ├ 工具（内置工作区工具） │   │
│  │  ─ 模型选择   │        │  ├ MCP（工具包管理）     │   │
│  │  ─ 目标选择   │        │  ├ 工作流（大型智能体）   │   │
│  │    (智能体/LA)│        │  ├ 记忆管理             │   │
│  │             │        │  └ MCP 设计（统一工具接入）│   │
│  └────────────┘        └───────────────────────┘   │
│                                                    │
│  智能体学习：不在本架构内（后续智能体设计内容）              │
└──────────────────────────────────────────────────┘
          │ 编译期烘焙（导出器 = 编译器）
┌──────────────────────────────────────────────────┐
│ 导出物 LA 包：manifest + executor + transports      │
│   + 内嵌 runtime + web 控制台 + 记忆空间              │
└──────────────────────────────────────────────────┘
```

模块职责：

| 模块 | 归属 | 职责 |
|------|------|------|
| 会话 | 大分层一 | 聊天窗口为核心：可自由选择目标（智能体 / 工作流 LA）、展示可展开思考痕迹、模型选择、会话配置保存 |
| 工作区 | 大分层二 | 任务配置与资源管理区，承载下方全部功能模块 |
| 智能体 | 工作区 | agent 文件（.agent.js/.agent.py）的管理、分类、简介、编辑 |
| 工具 | 工作区 | 内置工作区工具（list_dir / read_file / write_file 等） |
| MCP | 工作区 | MCP 工具包管理（.mcp.js/.mcp.py，经 MCP 与大模型连接） |
| 工作流（大型智能体） | 工作区 | 可视化流水线，编译导出为 LA 包 |
| 记忆管理 | 工作区 | 记忆读写/策略/账本/视图（见第四节） |
| MCP 设计 | 工作区 | 横切设计：所有工具（内置/工作流/记忆）统一经 MCP 协议暴露的标准化层 |

保持现有边界：生成器=编译器（编译期固化 I/O 契约/智能体/图），运行期薄；LA 黑盒内外分离；多目标输出（当前先只做 Python 目标，未来可加 JS）。

---

## 四、记忆管理（最大新课题）

### 4.1 核心原则（来自笔记）

1. **记忆放 md 文件**，可读可改，绝不做成黑盒（与导出物同目录：`memory/*.md`）。
2. **场景化策略**：不同语境用不同管理模式（电商客服 vs 电机调参，策略可配置/可插拔）。
3. 精确调用与状态覆写：精确查字段；旧数据确认无用后删除或打时间戳。
4. 记忆最小可调用闭包——**系统三件套**：
   - `policy`：控制策略——什么时候读写、什么时候自动遗忘；
   - `ledger`：原始账本——只追加，记录读写动作用于溯源；
   - `views`：派生视图——把死数据转为 LLM 可读的向量索引 / 图谱 / 时间线。
5. 双时态：区分 `valid time`（现实生效时间）与 `transaction time`（写入时间），检索带时间切片。
6. 从陈述性事实 → 程序性技能：把试错修复成功的交互轨迹去废话化，压缩成一键执行的宏指令。
7. 夜间 dream 模式：指定时间段复盘当天工作，对同类踩坑记录去重合并，有新解法直接覆盖。

### 4.2 参考架构取舍（笔记调研）

| 架构 | 借鉴点 | 取舍 |
|------|--------|------|
| ChatGPT 四层 | 会话元数据/用户档案卡(json)/近期摘要/滑动窗口 | 用户档案卡与近期摘要适合做**会话层轻量记忆** |
| 目的-方案-结果-痛点 | 只记四元组 + 错误解法进 skill + 打 tag，传统 SQL 即可 | **最贴合"轻量可落地"**：与 md 文件 + 简单检索天然契合 |
| text2mem | LLM 分析意图→12 原子操作→映射 sqlite；5 元组 JSON 契约；lock 四层保护 | 原子操作层抽象值得做；sqlite 可作 views 的物化层 |
| Mem0 | 语义/情景/程序性三类记忆；双存储(向量+图)；双层 prompt 提取；多层隔离 | 语义/情景/程序性分类吸收进 policy；向量库对高频不友好，**不引入** |
| Letta | core(嵌 system prompt)/archival(磁盘)/recall(log)；sleeping agent 反思 | 分层思想并入三件套；git 数据源太重，**不引入** |
| REME | 增量监控：仅对新增内容做 embedding；when-to-use 与 content 分离 | 轻量化，适合 md 文件方案 |
| memU | 双 agent 异步 + 共享队列 + bot 后台归档 + 显著性计数器 | 计数器思想进 policy；bot 异步暂缓 |

### 4.3 建议方案（最小可落地版）

**存储**：纯文件优先——`memory/` 目录下 md 文件（人可读、可审计、导出物自带）。索引用"简单 SQL（sqlite）"做 views 物化缓存，不引入向量库。

**文件结构（按场景划分）**：

```
memory/
├── policy.md            # 场景策略：读写时机/遗忘规则/检索规则（人可编辑）
├── ledger.md            # 原始账本：只追加，每次读写留痕（溯源）
├── facts/               # 陈述性事实（按主题分文件，md）
│   └── user_profile.md  # 用户档案卡（json 片段，随时覆写，绝不模棱两可）
├── episodes/            # 情景记忆（具体事件，含双时态时间戳）
├── skills/              # 程序性技能（踩坑修复轨迹 → 压缩后的宏指令，打 tag）
└── views/               # 派生视图（时间线/索引，由 dream 或查询时生成）
```

**三件套最小实现**：
- 检索：LLM 意图 → 原子操作（retrieve/summarize/append/update/delete/forget）→ 落到 md/sqlite；
- policy 决定每个场景默认开放哪些操作与遗忘阈值；
- ledger 由所有写操作自动追加（只 append）；
- views 由 dream 模式 / 查询时惰性生成（时间线、topic 索引）。

**双时态**：md 文件内统一 `[[valid:…]]` / `[[txn:…]]` 头部，检索工具按时间切片过滤。

**场景化**：`policy.md` 分场景模板（默认 / 电商客服 / 电机调参…），界面"智能体分类"可绑定记忆策略；导出 LA 包时把对应策略与 memory 目录一并烘焙。

**MVP 范围**（第一版只做）：
1. 会话/工作区可调用的 `memory_*` 工具（read/write/append/search/forget）。
2. 存 md + 简单全文检索（sqlite FTS5 或纯文本扫描）。
3. 三件套最小闭环：policy(默认模板) + ledger(自动追加) + views(时间线生成)。
4. dream 模式：夜间定时任务，同主题去重合并（先手工触发，再定时）。

---

## 五、MCP 设计（工具统一接入层）

> MCP.txt：MCP 是上下文协议，**所有工具都需要通过 MCP 与大模型连接**。本模块即架构分层中的「MCP 设计」。

现状：`tools.js` 有"内置工具(list_dir/read_file/write_file) + run_workflow + MCP 工具"三条通道。

方案：建立统一**工具适配层**，内置工具/工作流/记忆也以"虚拟 MCP"形态暴露（同样的 name/description/parameters/handler 契约），LLM 侧只见 MCP 一个入口。收益：
- 导出的 LA 包只依赖一份 MCP 协议，外部生态接入成本一致；
- 未来可对接真实 MCP Server（stdio/HTTP/SSE 传输），复用同一适配层。

优先级：中（记忆工具也会走这层）。

---

## 六、智能体学习（不纳入本架构）

笔记列出的方向：强化/监督/无监督/少样本/在线/基于记忆学习；SICA（Alpha Evolve / open Evolve）自我改进编码智能体。

**结论：不在本架构与路线内。** 用户定调（2026-08-19）：智能体学习属**后续智能体设计内容**，先不管。保留笔记备查，不排期。

---

## 七、实施路线（建议优先级）

| 里程碑 | 内容 | 依赖 |
|--------|------|------|
| **M1 体验闭环** ✅ | ① 会话可选择"智能体/工作流(大型智能体)"且配置可保存；② 工作区对话窗口；③ 可展开思考痕迹；④ 模型选择（deepseek-v4-pro / deepseek-v4-flash） | 已完成 |
| **M2 导出增强** ✅ | ⑤ 导出物 API key 首次配置页（保存到 data/config.json）；⑥ 导出为单个 exe（PyInstaller onefile，冻结模式 data_root/bundle_root + 配置页/控制台/记忆全内置）；⑦ 导出物内置记忆空间骨架（memory/ + policy/ledger + 四子目录） | 已完成；补充：工作流智能体节点支持链接 MCP 工具/内置工具/技能（技能=md 指令片段，注入节点 system prompt） |
| **M3 记忆 MVP** | ⑧ `memory_*` 工具 + md 存储 + 三件套最小闭环 + 场景策略（policy.md） | M2（⑦ 提供空间） |
| **M4 记忆进阶** | ⑨ dream 模式（夜间复盘去重）；⑩ 双时态完整检索；⑪ 程序性技能压缩宏指令 | M3 |
| **M5 MCP 设计** | ⑫ 内置工具/工作流/记忆统一走"虚拟 MCP"适配层；⑬ 真实 MCP Server 接入 | M3 |
| **W1 工作区基础** | ⑭ 内嵌编辑器 ✅；⑮ 全局搜索（文件名+内容）；⑯ `.harnessignore` 忽略规则；⑰ 路径面包屑/双击打开 | M1 |
| **W2 工作区编辑器** | ⑱ 多标签页；⑲ Diff 视图；⑳ 图片/Markdown 预览；㉑ 文件复制/粘贴/移动 | W1 |
| **W3 智能协作** | ㉒ Git 集成（含 AI 一键提交信息）；㉓ 工作区代码索引（#Code 语义引用）；㉔ 项目级规则/记忆；㉕ Remote SSH 远程开发 | W2 |
| **T1 团队基础** | ㉖ 账号/项目邀请；㉗ Git 工作台（clone/status/diff/commit/push/pull）；㉘ AI 提交信息 | W3 |
| **T2 资产共享** | ㉙ 智能体/工作流/MCP 导出导入包；㉚ 云端资产库（跨设备/成员一键导入） | T1 |
| **T3 实时协同** | ㉛ 在线共享会话（多人同看 AI 流）；㉜ 文件实时同步（CRDT/OT）；㉝ 任务分派 | T2 |

> 智能体学习不在路线内（见第六节）。
> W 系列与 T 系列详见「九、工作区完善清单」与「十、团队开发方案」。

---

## 八、待评审决策点

1. 导出 exe 方式：PyInstaller 把 Python 包打成单 exe（体积大、解包慢） vs 保持"目录 + start.bat"再包 NSIS（轻快）——倾向后者。
2. 记忆检索索引：sqlite FTS5 vs 纯文件扫描 vs 引入向量库——倾向 sqlite FTS5（轻量、可审计）。
3. 记忆策略是否与"智能体分类"绑定（分类 = 策略分组）——倾向绑定，复用现有分类体系。
4. dream 模式先手动触发，还是直接定时（需常驻进程）——倾向先手动 + 会话内触发。

---

## 九、工作区完善清单（参照 TraeWork，2026-08-19 用户确认按 P0→P1→P2 全做）

> 参照对象：TraeWork 的 Work 模式 / TraeCode 的 Code 模式工作环境能力（打开项目、多文件编辑、源代码管理、代码索引、远程开发、忽略文件、跨设备同步、自定义智能体分享等）。

**现状盘点**：已有 打开/切换文件夹（**重启自动恢复**）、文件树（新建/重命名/删除/文件夹）、**内嵌编辑器（语法高亮/保存/Ctrl+S/脏标记）**、外部编辑器打开、**底部工作区对话栏（智能体/工作流目标 + 流式回复 + 思考痕迹）**、AI 读写工具（list_dir/read_file/write_file）。

### P0 · 基础补全（W1）

| # | 功能 | 技术要点 |
|---|------|---------|
| 1 | **工作区内直接编辑** | 只读预览改为内嵌编辑器，复用 CodeEditor（语法高亮 / Ctrl+S / 脏标记 / IME 组合输入安全）；`workspace:write` 已有 |
| 2 | **全局搜索** | 文件名搜索 + 文件内容搜索；主进程加 `workspace:search(query, mode)`，扫树 + 文本扫描（200KB 上限沿用）；尊重忽略规则 |
| 3 | **忽略文件** | `.harnessignore`（类 .gitignore 语法简化版）；同时约束文件树显示与 AI 工具读写范围（与 `ignoreDir()` 合并） |
| 4 | **面包屑 / 双击打开** | 树顶显示当前路径、点击层级跳转；双击文件直接进编辑器；`tree` 支持指定展开深度 |

### P1 · 编辑器与浏览增强（W2）

| # | 功能 | 技术要点 |
|---|------|---------|
| 5 | **多标签页** | 顶部标签栏，每文件一个标签；脏状态圆点；关闭确认；Ctrl+Tab 切换 |
| 6 | **Diff 视图** | 文件与暂存/历史版本对比（为 Git 打底），行级增删高亮 |
| 7 | **图片 / Markdown 预览** | 图片直接渲染（file:// 读取），`.md` 用现有 Markdown 组件渲染（react-markdown 已有） |
| 8 | **复制 / 粘贴 / 移动** | 文件树右键补齐：复制路径、剪切、粘贴、移动到（目录选择弹窗）、多选批量 |

### P2 · 智能与协作（W3）

| # | 功能 | 技术要点 |
|---|------|---------|
| 9 | **Git 集成** | 初始化仓库 / clone；status / diff / stage / commit / push / pull；冲突可视化；AI 一键总结变更生成提交信息（参考 TraeCode 源代码管理） |
| 10 | **工作区代码索引** | 增量扫描建立倒排索引（文件名+标识符+内容摘要），AI 侧 #Code 语义引用与跨文件理解 |
| 11 | **项目级规则 / 记忆** | 工作区内 `RULES.md` 自动注入 AI 上下文；项目记忆接入现有记忆方案（第四节） |
| 12 | **Remote SSH 远程开发** | 连接远程主机，安装轻量服务端，本地界面操作远程文件（参考 TraeCode Remote SSH） |

---

## 十、团队开发方案（联网，2026-08-19）

> 用户决策：**先写方案文档**再动工；落地模式倾向 Git 底座起步 + 资产共享并行 + 实时协同进阶。

### 10.1 目标

多成员共享同一工作区 / 智能体 / 工作流，在线协作开发；AI 会话与产物跨设备、跨成员同步；API Key 等敏感配置永不共享。

### 10.2 架构（三阶段递进）

```
阶段一（T1）Git 底座           阶段二（T2）资产共享         阶段三（T3）实时协同
┌──────────────┐    ┌────────────────────┐    ┌──────────────────────┐
│ 工作区 = Git 仓库 │    │ 智能体/工作流/MCP      │    │ 在线共享会话（多人同看   │
│ 内置 Git 工作台   │    │ 导出为 JSON 资产包       │    │ AI 流/光标/文件同步     │
│ (clone/status/ │    │ 云端资产库，成员一键导入  │    │ CRDT/OT 实时同步       │
│ diff/commit/  │    │ (对应 TraeWork 分享能力) │    │ 任务分派               │
│ push/pull)    │    └────────────────────┘    └──────────────────────┘
└──────────────┘
 冲突解决/历史/权限    跨设备/跨成员复用          实时协作（需自建服务器）
 现成、零服务器成本      云端只存资产包，无代码       Node + WebSocket 轻量服务
```

### 10.3 分阶段内容

**T1 · 团队基础（Git 底座）**
- 账号 / 项目邀请：工作区关联远端（GitHub / Gitee / 自建 Git），成员 clone 即协作；
- 内置 Git 工作台：status / diff / stage / commit / push / pull / 冲突可视化；
- AI 一键生成提交信息（总结变更 → 建议 message → 确认提交）；
- 交付物：`W3` 的 Git 集成是其前置。

**T2 · 资产共享（与 T1 并行）**
- 智能体 / 工作流 / MCP 统一导出为 JSON 资产包（manifest + 代码 + 配置，不含 API Key）；
- 云端资产库：上传 / 搜索 / 一键导入（成员免手工复制）；对应 TraeWork 跨设备同步与自定义智能体分享；
- 交付物：LAG 导出器复用其序列化契约。

**T3 · 实时协同（进阶）**
- 自建轻量服务（Node + WebSocket）：工作区文件 CRDT/OT 同步、成员在线状态与光标；
- 在线共享会话：多人进入同一会话，共同观察 AI 流式输出并分派子任务；
- 任务分派：会话中创建任务 → 成员认领 → 变更经 Git 合入；
- 需要：服务器部署、鉴权（令牌/账号体系）、存储与审计。

### 10.4 安全底线

1. **API Key 永不进仓库 / 资产包 / 云端**——只存本地 `settings.json`（现有机制）；
2. 成员鉴权：Git 用远端凭证；资产库与实时服务用令牌（复用现有 `apiToken` 体系）；
3. 操作审计：Git 历史即审计；实时协同服务记录操作日志；
4. `.harnessignore` 与 `.gitignore` 双保险：敏感目录（data、密钥）默认忽略。

### 10.5 依赖与衔接

- W3（Git 集成 / 代码索引）是 T1 前置；T2 依赖 LAG 导出器的序列化契约；
- 记忆（第四节）作为项目级能力随工作区共享，成员可各自本地化；
- 实时协同（T3）工作量最大，放最后，需明确是否自建服务器或对接现成服务。
