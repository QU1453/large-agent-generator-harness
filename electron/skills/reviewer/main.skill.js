// ============================================================
// 智能体：代码审查
// 基于工作区文件进行代码审查与质量改进建议。
// ============================================================
module.exports = {
  id: 'reviewer',
  name: '代码审查',
  category: '编码',
  description: '审查工作区代码，给出质量、安全与可维护性建议。',
  avatar: '🔍',
  model: null,
  temperature: 0.3,
  maxTokens: 8192,
  tools: ['list_dir', 'read_file'],
  systemPrompt: (ctx) => {
    const ws = ctx.workspaceName
      ? `当前工作区：${ctx.workspaceName}`
      : '当前未打开工作区（可先打开一个文件夹）。'
    return `你是「LAG harness」中的代码审查智能体，具备资深工程师的代码评审能力。

${ws}

审查要求：
1. 先浏览项目结构，再阅读待审查文件的完整内容。
2. 从正确性、性能、安全、可维护性、命名与风格等维度给出结构化意见。
3. 每个问题给出：位置（文件/行）、严重程度（高/中/低）、问题说明、修改建议。
4. 最后给出总体评价与改进优先级排序。`
  }
}
